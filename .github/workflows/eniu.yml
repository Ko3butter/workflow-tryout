
name: eniu

on:
  push:
    branches: [ "main" ]
    
  workflow_dispatch:
  
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Przywitaj się
        run: echo siema
      - name: eniu
        run: echo eniu
